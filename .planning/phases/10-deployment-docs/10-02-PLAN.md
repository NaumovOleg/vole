---
phase: 10-deployment-docs
plan: 02
type: docs
wave: 2
depends_on: [10-01]
files_modified:
  - README.md
autonomous: true
must_haves:
  truths:
    - "An outsider can deploy the whole service from the README alone"
    - "Route 53 DNS setup is documented step by step"
  artifacts:
    - root README.md
  key_links:
    - "close-out: REPOSITORY.md / cli/README already cover the CLI"
---

<objective>
Write the root README: what Vole is, architecture, full deployment walkthrough
(CDK, DNS, UI, CLI), usage and admin notes.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-deployment-docs/10-PHASE.md
@/Users/oleg/Documents/projects/http-tunell/cli/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: root README</name>
  <files>README.md</files>
  <behavior>Sections: What is Vole (one paragraph + traffic flow); Architecture
  (components table: WS API+Lambdas, relay Lambda URL via CloudFront wildcard,
  auth REST, DynamoDB tables, S3+CloudFront UI); Prerequisites (Node 22+, Bun,
  AWS CLI with creds, CDK); Deploy: 1) `cd infra && cdk bootstrap && cdk deploy`
  — with ADMIN_IDENTIFIERS env example; 2) Route 53 manual: ACM validation
  CNAMEs (vole.sh + *.vole.sh) then record sets → UiDistributionDomain (apex)
  and TunnelDistributionDomain (wildcard); 3) `scripts/deploy-ui.sh`; 4) CLI:
  note `bun build --target=node cli/src/index.ts` (npm publish comes later),
  `vole authtoken <token>`, `vole http 3000`, multi-tunnel; Walkthrough
  register → token → tunnel → request → logs → admin block; Tests/Tools
  (bun test, shared/infra/cli); Repo layout (monorepo dirs). Keep it tight.</behavior>
  <implementation>Markdown, code fences for commands. No comments rule doesn't
  apply to prose but stay lean — no boilerplate, no license section unless asked.</implementation>
  <verify>Manually: every path/command in the file exists (cd infra, scripts/deploy-ui.sh, cli build)</verify>
  <done>README alone reproduces a deploy</done>
</task>

</tasks>

<verification>
All commands referenced resolve to real files/paths in the repo.
</verification>

<success_criteria>
- Full deploy + usage documented end to end
- Route 53 steps concrete (two record types, exact values from cdk outputs)
</success_criteria>

<output>
After completion, create `.planning/phases/10-deployment-docs/10-02-SUMMARY.md`
</output>