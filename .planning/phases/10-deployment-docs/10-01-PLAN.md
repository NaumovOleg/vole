---
phase: 10-deployment-docs
plan: 01
type: feature
wave: 1
depends_on: []
files_modified:
  - infra/lib/vole-stack.ts
  - scripts/deploy-ui.sh
autonomous: true
must_haves:
  truths:
    - "One command builds the UI, syncs it to S3 and invalidates CloudFront"
  artifacts:
    - scripts/deploy-ui.sh + UiDistributionId output
  key_links:
    - "10-02 documents the script"
---

<objective>
Add a one-command UI deploy script backed by CDK stack outputs.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-deployment-docs/10-PHASE.md
@/Users/oleg/Documents/projects/http-tunell/infra/lib/vole-stack.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: CDK output</name>
  <files>infra/lib/vole-stack.ts</files>
  <behavior>Add CfnOutput UiDistributionId = uiDistribution.distributionId next to
  UIBucketName/UiDistributionDomain.</behavior>
  <implementation>Single line with the others; no comments.</implementation>
  <verify>cd infra && npx cdk synth --quiet && npx tsc --noEmit</verify>
  <done>Output exists for the script</done>
</task>

<task type="auto">
  <name>Task 2: deploy-ui.sh</name>
  <files>scripts/deploy-ui.sh</files>
  <behavior>set -euo pipefail. STACK_NAME=${STACK_NAME:-VoleStack}. Resolve
  bucket + dist id via `aws cloudformation describe-stacks --stack-name $STACK_NAME
  --query "Stacks[0].Outputs[?OutputKey==\`$KEY\`].OutputValue" --output text`
  (fail with a hint if empty). Then: npm --prefix ui run build,
  aws s3 sync ui/dist s3://$BUCKET --delete,
  aws cloudfront create-invalidation --distribution-id $DIST --paths '/*'.
  Print the UI URL when done.</behavior>
  <implementation>POSIX bash, no deps beyond AWS CLI. No comments.</implementation>
  <verify>bash -n scripts/deploy-ui.sh; run with no creds → clean error message</verify>
  <done>One command deploys the UI</done>
</task>

</tasks>

<verification>
bash -n passes; without AWS creds the script fails with a clear message before
doing anything destructive.
</verification>

<success_criteria>
- Build → sync → invalidate in a single command
- Missing stack/creds produce a readable error
</success_criteria>

<output>
After completion, create `.planning/phases/10-deployment-docs/10-01-SUMMARY.md`
</output>