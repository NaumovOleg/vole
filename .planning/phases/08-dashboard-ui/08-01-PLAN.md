---
phase: 08-dashboard-ui
plan: 01
type: feature
wave: 1
depends_on: []
files_modified:
  - ui/package.json
  - ui/tsconfig.json
  - ui/vite.config.ts
  - ui/index.html
  - ui/src/main.tsx
  - ui/src/App.tsx
  - ui/src/api.ts
  - ui/src/AuthScreen.tsx
  - ui/src/styles.css
  - ui/.gitignore
autonomous: true
must_haves:
  truths:
    - "Vite + React app builds from ui/ and shows a working login/register screen"
    - "API calls carry the JWT and surface readable errors"
  artifacts:
    - ui/: scaffold + auth screens + api wrapper
  key_links:
    - "dashboard screens consume api.ts in 08-02"
---

<objective>
Scaffold the React dashboard and implement the auth screens wired to
`/auth/register`, `/auth/login`, `/auth/me`.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-dashboard-ui/08-PHASE.md
@/Users/oleg/Documents/projects/http-tunell/infra/lib/lambdas/auth-handler/index.ts (API contract)
</context>

<tasks>

<task type="auto">
  <name>Task 1: scaffold + api wrapper</name>
  <files>ui/package.json, ui/tsconfig.json, ui/vite.config.ts, ui/index.html, ui/src/main.tsx, ui/src/api.ts, ui/.gitignore</files>
  <behavior>Vite React TS app: react, react-dom, @vitejs/plugin-react, typescript, vite.
  api.ts: request(method, path, body?) → fetch `${VITE_API_URL ?? ''}/${path}`,
  JSON headers, Authorization from localStorage 'vole_jwt'; throws Error(message)
  from {error} body or status; api() helpers: register, login, me. main.tsx mounts App.
  index.html: title 'Vole', skeleton div.</behavior>
  <implementation>Standard vite scaffold by hand (no create-vite); no router, no
  extra deps. skipBuild default; outDir default dist. No comments.</implementation>
  <verify>cd ui && bun install && npx tsc --noEmit && npm run build</verify>
  <done>App builds; api wrapper typed</done>
</task>

<task type="auto">
  <name>Task 2: auth screens</name>
  <files>ui/src/App.tsx, ui/src/AuthScreen.tsx, ui/src/styles.css</files>
  <behavior>App: on mount, if localStorage jwt → me() → logged in (else clear);
  AuthScreen: single form with mode toggle (login/register), identifier + password
  fields (password min 8 client-side for register), submit → api.login/register →
  store jwt → onAuthed. Error message shown inline. Styles: centered card, plain
  dark theme, no framework.</behavior>
  <implementation>App holds {user} | null; renders AuthScreen or Dashboard (stub
  placeholder for 08-02). No comments.</implementation>
  <verify>cd ui && npx tsc --noEmit && npm run build</verify>
  <done>Login/register round-trip works against live API</done>
</task>

</tasks>

<verification>
Build green; manual: run `npm run dev` and register + login flow if an API is reachable.
</verification>

<success_criteria>
- Register/login UI completes the round-trip (JWT persisted)
- Invalid credentials show a readable inline error
</success_criteria>

<output>
After completion, create `.planning/phases/08-dashboard-ui/08-01-SUMMARY.md`
</output>