---
phase: 08-dashboard-ui
plan: 03
type: polish
wave: 3
depends_on: [08-02]
files_modified:
  - ui/src/Dashboard.tsx
  - ui/src/styles.css
  - ui/README.md
autonomous: true
must_haves:
  truths:
    - "Dashboard stays fresh while open; errors are readable; state survives reload"
  artifacts:
    - ui/src/Dashboard.tsx: auto-refresh + error banner + relative time
    - ui/README.md: dev/build instructions
  key_links:
    - "phase 10 delivers the deploy script; this plan ends the UI work"
---

<objective>
Polish: 10s auto-refresh while the dashboard tab is visible, readable fetch
errors, relative timestamps, logout, README.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-dashboard-ui/08-PHASE.md
@ui/src/Dashboard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: auto-refresh + polish</name>
  <files>ui/src/Dashboard.tsx, ui/src/styles.css, ui/README.md</files>
  <behavior>- useEffect interval 10s: refetch all panels only when document.visibilityState
    === 'visible' (skip hidden tabs); guard concurrent fetches
  - Error banner div (red) shown on any failed fetch, auto-clears on success
  - Timestamps rendered as relative (e.g. '3m ago') via small helper; full ISO in title attr
  - Logout button clears localStorage and returns to auth screen
  - README: prerequisites (bun), `bun install`, `npm run dev`, `VITE_API_URL`,
    `npm run build` (out: dist/)</behavior>
  <implementation>Plain hooks; relative time helper in Dashboard.tsx. No comments.</implementation>
  <verify>cd ui && npx tsc --noEmit && npm run build</verify>
  <done>Dashboard auto-refreshes, errors readable, README done</done>
</task>

</tasks>

<verification>
Build green.
</verification>

<success_criteria>
- Auto-refresh respects tab visibility; no concurrent-fetch pileup
- All fetch failures readable; logout returns to auth screen
</success_criteria>

<output>
After completion, create `.planning/phases/08-dashboard-ui/08-03-SUMMARY.md`
</output>