---
phase: 08-dashboard-ui
plan: 02
type: feature
wave: 2
depends_on: [08-01]
files_modified:
  - ui/src/Dashboard.tsx
  - ui/src/api.ts
  - ui/src/styles.css
autonomous: true
must_haves:
  truths:
    - "Dashboard renders tokens, connections, and logs from the live API"
    - "Token create displays the raw token once; revoke removes it"
  artifacts:
    - ui/src/Dashboard.tsx: three panels
    - ui/src/api.ts: listTokens/createToken/revokeToken/connections/logs
  key_links:
    - "refresh UX in 08-03"
---

<objective>
Dashboard panels: tokens CRUD (create/revoke/copy), connections table, logs table.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-dashboard-ui/08-PHASE.md
@ui/src/api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: dashboard panels</name>
  <files>ui/src/Dashboard.tsx, ui/src/api.ts, ui/src/styles.css</files>
  <behavior>Dashboard: header (email + logout), refresh button; three sections:
  - Tokens: list (createdAt formatted, last 4 chars …?), "Create token" button →
    shows raw token once in a highlighted box with Copy button (navigator.clipboard);
    each row Revoke → api.revokeToken → refetch
  - Connections: table subdomain | type | local port | status (active) | createdAt
  - Logs: table method | path | status | statusCode | latency ms | time — empty
    state text when none
  All data loaded on mount + on refresh; 401 → onUnauthed (back to auth screen).
  api.ts additions listTokens/createToken/revokeToken/getConnections/getLogs.
  Layout: max-width 900px, tables striped, plain CSS.</behavior>
  <implementation>React hooks only. createToken response { token } — render once and
  replace with '•••' on next render. No comments.</implementation>
  <verify>cd ui && npx tsc --noEmit && npm run build</verify>
  <done>Dashboard works end to end</done>
</task>

</tasks>

<verification>
Build green; manual against live API if reachable.
</verification>

<success_criteria>
- Tokens: create shows raw once + copy, revoke works
- Connections and logs tables render server data
</success_criteria>

<output>
After completion, create `.planning/phases/08-dashboard-ui/08-02-SUMMARY.md`
</output>