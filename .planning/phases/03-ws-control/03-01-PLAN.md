---
phase: 03-ws-control
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - shared/src/protocol.ts
  - shared/src/protocol.test.ts
autonomous: true
must_haves:
  truths:
    - "Every WS message is a typed frame: {t, id, d} with a fixed set of types"
    - "Malformed frames fail loudly (throw), not silently corrupt"
    - "The frame contract lives in shared/ so CLI (Bun) and Lambda (Node) use one source of truth"
  artifacts:
    - shared/src/protocol.ts (FrameType, Frame, encodeFrame, parseFrame, hello/ready/ping/pong helpers)
    - shared/src/protocol.test.ts
  key_links:
    - "shared/src/protocol.ts imported by ws-handler (plan 03-02) and by the CLI (phase 4)"
---

<objective>
Build the WS frame protocol as TDD in the shared workspace: typed JSON frames {t, id, d} with strict parsing.

Purpose: One protocol contract used by Lambda, CLI and probe — phase 4 relies on it; malformed frames must never be silently ignored.
Output: protocol.ts + protocol.test.ts (bun test).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-auth/02-auth-01-SUMMARY.md (token hashing — connection auth uses it)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD frame protocol</name>
  <files>shared/src/protocol.ts, shared/src/protocol.test.ts</files>
  <behavior>RED: write shared/src/protocol.test.ts (run `bun test` from shared/) with:
  - FrameType set: hello, ready, ping, pong, tunnel-open, tunnel-close, request, response, data, error
  - encodeFrame('ping', 'id1') → parseFrame round-trips to same {t, id}
  - encodeFrame('hello', 'id1', {version: 1}) round-trips payload
  - parseFrame rejects: non-JSON, JSON without t, unknown t, missing/non-string id — all throw
  - helloFrame(version) / readyFrame() / pingFrame() / pongFrame(id) helpers produce correct frames
  - parseFrame accepts unknown extra payload fields (forward compat)</behavior>
  <implementation>GREEN: create shared/src/protocol.ts: export const FRAME_TYPES = [...] as const; type FrameType = typeof FRAME_TYPES[number]; interface Frame<T> { t: FrameType; id: string; d?: T }. encodeFrame(t, id, d) → JSON.stringify({t, id, ...(d !== undefined ? {d} : {})}). parseFrame(raw): JSON.parse (catch → throw 'invalid frame'), validate t ∈ FRAME_TYPES and typeof id === 'string' and id non-empty, else throw 'invalid frame'; return typed frame. Helpers: helloFrame(version), readyFrame(), pingFrame(), pongFrame(id), errorFrame(id, message) with payload {error}. No comments.</implementation>
  <verify>cd shared && bun test — all green</verify>
  <done>Protocol round-trips, rejects malformed input, helpers exist</done>
</task>

</tasks>

<verification>
bun test passes fully in shared/.
</verification>

<success_criteria>
- All 10 frame types defined; round-trip and rejection behaviors proven by tests
- Protocol importable by both Lambda (Node) and CLI (Bun)
</success_criteria>

<output>
After completion, create `.planning/phases/03-ws-control/03-ws-control-01-SUMMARY.md`
</output>
