---
phase: 03-ws-control
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified:
  - infra/scripts/ws-probe.ts
  - infra/package.json
autonomous: true
user_setup:
  - service: aws
    why: "Run the probe against the deployed WS API"
    env_vars:
      - name: WS_URL
        source: "cdk output WebSocketUrl (wss://...)"
      - name: VOLE_TOKEN
        source: "created via POST /tokens after deploy"
must_haves:
  truths:
    - "A script can connect, HELLO→READY, PING→PONG, and disconnect cleanly"
    - "Invalid token is rejected at handshake (WS closes)"
    - "Connection row appears in DynamoDB on connect and disappears on disconnect"
  artifacts:
    - infra/scripts/ws-probe.ts (bun client: connect/hello/ping/disconnect + failure case)
  key_links:
    - "probe uses shared protocol frames — same contract the CLI will use"
    - "verify via aws dynamodb get-item on CONNECTIONS_TABLE"
---

<objective>
Build a Bun probe client that exercises the deployed control plane end-to-end: valid connect + HELLO/READY + PING/PONG, invalid-token rejection, row lifecycle in DynamoDB.

Purpose: Proves CORE-01/CORE-06/PROTO-03 against the real deployment and doubles as the reference client for phase 4's CLI.
Output: infra/scripts/ws-probe.ts.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ws-control/03-ws-control-01-SUMMARY.md (protocol)
@.planning/phases/03-ws-control/03-ws-control-02-SUMMARY.md (handler — if exists)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the probe client</name>
  <files>infra/scripts/ws-probe.ts, infra/package.json</files>
  <action>Create infra/scripts/ws-probe.ts (Bun). Args: URL, token. Uses Bun-native WebSocket (global in Bun, no deps). Flow: connect wss URL with query `?token=<token>`; on open send helloFrame(1); on message: parseFrame — on ready print 'READY ✓', then send pingFrame every 5s (setInterval) and on pong print 'PONG ✓' once and exit 0 after 2 successful pongs; on close print reason; on error exit 1. Invalid-token mode: `--expect-reject` flag — connect with bad token, expect close within 5s → exit 0 if closed, else exit 1. Timeout: exit 1 after 30s with no READY. Add npm script in infra/package.json: "probe": "bun run scripts/ws-probe.ts". Typecheck: add scripts to infra tsconfig include (already includes lib + bin — add scripts). Bun WebSocket needs no @types (bun types via bun; tsc exclude scripts or add "bun-types" — simplest: include scripts in tsconfig exclude? No: keep tsc clean by importing from 'bun:websocket'? Plain global WebSocket in bun — types come from @types/ws? To keep tsc simple, exclude scripts/ from infra tsconfig).</action>
  <verify>npx tsc --noEmit still clean; `bun run scripts/ws-probe.ts --help` prints usage (no network needed)</verify>
  <done>Probe runs; usage + connection flow implemented</done>
</task>

</tasks>

<verification>
After user deploys: `bun run scripts/ws-probe.ts <wss-url> <token>` prints READY ✓ + PONG ✓ and exits 0; `--expect-reject` with a bogus token exits 0. Then `aws dynamodb get-item --table-name VoleStack-ConnectionsTable...` shows the row while connected.
</verification>

<success_criteria>
- Full happy path verified against deployed API
- Rejection path verified; row lifecycle verified
</success_criteria>

<output>
After completion, create `.planning/phases/03-ws-control/03-ws-control-03-SUMMARY.md`
</output>
