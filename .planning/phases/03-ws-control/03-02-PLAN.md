---
phase: 03-ws-control
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - infra/lib/lambdas/ws-handler/index.ts
  - infra/package.json
autonomous: true
must_haves:
  truths:
    - "CLI connects with ?token=<vole_ token> — valid token accepted, connection registered"
    - "Invalid/revoked token rejected at $connect (connection never established)"
    - "Disconnect removes the connection record; TTL cleans zombies"
    - "HELLO answered with READY; PING answered with PONG; lastSeenAt updated"
  artifacts:
    - infra/lib/lambdas/ws-handler/index.ts ($connect/$disconnect/$default with token auth, frame handling)
  key_links:
    - "token lookup via TokensTable tokenHashIndex (hashToken from auth core)"
    - "users blocked flag checked at connect"
    - "postToConnection via ApiGatewayManagementApiClient (grantManageConnections already in stack)"
    - "shared protocol imported for frame parsing"
---

<objective>
Implement the WS control plane handler: authenticated $connect, $disconnect cleanup, $default frame routing (HELLO→READY, PING→PONG, error on unknown).

Purpose: Delivers CORE-01, CORE-06, PROTO-03. Connections registry becomes the backbone for relay (phase 4) and dashboard (phase 7).
Output: ws-handler/index.ts fully implemented.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-auth/02-auth-01-SUMMARY.md (hashToken)
@.planning/phases/03-ws-control/03-ws-control-01-SUMMARY.md (protocol — if exists)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement $connect with token auth</name>
  <files>infra/lib/lambdas/ws-handler/index.ts, infra/package.json</files>
  <action>Rewrite infra/lib/lambdas/ws-handler/index.ts. Import { hashToken } from '../../auth-handler/core' (relative path; esbuild bundles it — do NOT move core yet; note: core imports bcryptjs which bundles fine) and frame helpers from '../../../../shared/src/protocol'. Also import DynamoDBDocumentClient (same pattern as auth-handler), ApiGatewayManagementApiClient + PostToConnectionCommand from '@aws-sdk/client-apigatewaymanagementapi', and ApiGatewayManagementApiClientConfig to derive endpoint from event: { region, endpoint: `https://${event.requestContext.domainName}/${event.requestContext.stage}` } — construct lazily. env: CONNECTIONS_TABLE, TOKENS_TABLE, USERS_TABLE already present.

$connect: token from event.queryStringParameters?.token. Missing → return {statusCode: 401}. hashToken(token) → Query TOKENS_TABLE tokenHashIndex, KeyCondition tokenHash = :h, Limit 1. Not found → 401. Get user by userId from USERS_TABLE; user.blocked === true → 401 (and delete token row so blocked users can't reuse). Put connection item: {connectionId, userId, tokenHash, connectedAt: Date.now(), expiresAt: Date.now() + 12*3600*1000 (TTL)}. Return {statusCode: 200}. Log only connectionId + userId.

$disconnect: Delete connection item by connectionId from CONNECTIONS_TABLE. Return {statusCode: 200} (never fail on already-missing row).

$default: body = event.body (JSON string from client). parseFrame(body) — on throw send error frame back via postToConnection and return {statusCode: 200} (keep connection alive; a malformed frame is not fatal). Frame routing:
  - hello: verify d.version matches PROTOCOL_VERSION from shared; reply readyFrame() via postToConnection; also update connection lastSeenAt.
  - ping: reply pongFrame(frame.id) via postToConnection; update lastSeenAt (Put with UpdateExpression SET lastSeenAt, expiresAt refresh).
  - anything else: reply errorFrame(frame.id, 'unsupported in this phase') — tunnel/request/data frames arrive in phase 4.
postToConnection failures: ignore GONE (client disconnected); log others. All DynamoDB ops via DocumentClient. Never log tokens.</action>
  <verify>cd infra && npx tsc --noEmit && bun test (existing) green && npx cdk synth exit 0</verify>
  <done>Handler implements all three routes with correct auth, cleanup and frame responses</done>
</task>

</tasks>

<verification>
Typecheck clean, synth clean. Integration via 03-03 probe after user deploy.
</verification>

<success_criteria>
- $connect: valid token → 200 + connection row; invalid → 401 (no row)
- $disconnect: row deleted
- $default: HELLO→READY, PING→PONG, malformed → error frame, connection survives
</success_criteria>

<output>
After completion, create `.planning/phases/03-ws-control/03-ws-control-02-SUMMARY.md`
</output>
