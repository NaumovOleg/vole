---
phase: 07-dashboard-api
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - infra/lib/lambdas/auth-handler/dashboard.ts
  - infra/test/dashboard.test.ts
autonomous: true
must_haves:
  truths:
    - "Dashboard responses are derived from raw DDB rows by pure functions"
    - "Logs are sorted newest-first and capped"
  artifacts:
    - infra/lib/lambdas/auth-handler/dashboard.ts: pure row→response mappers
    - infra/test/dashboard.test.ts
  key_links:
    - "auth-handler routes query DDB then call these (07-02)"
---

<objective>
Pure dashboard mappers (TDD): tunnel rows → connections response; log rows →
sorted, capped summary.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-dashboard-api/07-PHASE.md
@infra/lib/lambdas/auth-handler/index.ts
@infra/lib/lambdas/relay-handler/index.ts (log row shape)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: dashboard mappers (TDD)</name>
  <files>infra/lib/lambdas/auth-handler/dashboard.ts, infra/test/dashboard.test.ts</files>
  <behavior>RED: add infra/test/dashboard.test.ts (bun test).
  Tunnel rows (userIdIndex items): { subdomain, userId, connectionId, type, localPort, createdAt }.
  Log rows: { connectionId, requestId, method, path, status, statusCode?, createdAt,
  completedAt? }.
  - listConnections(tunnelRows): [{ subdomain, type, localPort, createdAt, status: 'active' }]
    — strips userId/connectionId, sorts by createdAt desc
  - logSummary(logRows, limit=50): sort by (completedAt ?? createdAt) desc, slice limit,
    map to { requestId, method, path, status, statusCode?, latency (completedAt-createdAt
    when present), time: completedAt ?? createdAt }
  - latency: pending rows (no completedAt) → latency null/absent
  - empty inputs → empty arrays; more rows than limit → capped
  - rows missing optional fields → no crash</behavior>
  <implementation>GREEN: infra/lib/lambdas/auth-handler/dashboard.ts with
  listConnections + logSummary (pure, no AWS imports). No comments.</implementation>
  <verify>cd infra && bun test</verify>
  <done>Mappers proven: shape, sorting, capping, missing fields</done>
</task>

</tasks>

<verification>
bun test passes fully in infra/.
</verification>

<success_criteria>
- Connections mapped correctly (own fields, sorted)
- Logs sorted newest-first, capped, latency computed
</success_criteria>

<output>
After completion, create `.planning/phases/07-dashboard-api/07-01-SUMMARY.md`
</output>