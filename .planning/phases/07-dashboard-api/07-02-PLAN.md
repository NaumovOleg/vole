---
phase: 07-dashboard-api
plan: 02
type: integration
wave: 2
depends_on: [07-01]
files_modified:
  - infra/lib/lambdas/auth-handler/index.ts
autonomous: true
must_haves:
  truths:
    - "GET /connections and GET /logs exist on the HttpApi with JWT auth"
    - "Responses are scoped to the authenticated user"
  artifacts:
    - infra/lib/lambdas/auth-handler/index.ts: two new routes
  key_links:
    - "mappers from 07-01; UI consumes these in phase 8"
---

<objective>
Add `GET /connections` and `GET /logs` to the auth-handler: JWT auth, own-data
queries, mapped responses.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-dashboard-api/07-PHASE.md
@infra/lib/lambdas/auth-handler/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: GET /connections</name>
  <files>infra/lib/lambdas/auth-handler/index.ts</files>
  <behavior>Route: 'GET /connections'. requireAuth → Query TunnelsTable userIdIndex by
  userId → listConnections(rows) → json(200, { connections: [...] }).</behavior>
  <implementation>Add switch case + fn. TUNNELS_TABLE env already in commonEnv.
  No comments.</implementation>
  <verify>npx tsc --noEmit && bun test && npx cdk synth</verify>
  <done>GET /connections wired</done>
</task>

<task type="auto">
  <name>Task 2: GET /logs</name>
  <files>infra/lib/lambdas/auth-handler/index.ts</files>
  <behavior>Route: 'GET /logs'. requireAuth → Query TunnelsTable userIdIndex (own
  tunnels) → collect distinct connectionIds → for each, Query LOGS_TABLE by
  connectionId (Limit 100, consistent read not needed) → merge rows →
  logSummary(rows, 50) → json(200, { logs: [...] }).
  No tunnels → empty array (no queries).</behavior>
  <implementation>Add switch case + fn; parallelize the per-connection queries with
  Promise.all. No comments.</implementation>
  <verify>npx tsc --noEmit && bun test && npx cdk synth</verify>
  <done>GET /logs wired</done>
</task>

</tasks>

<verification>
tsc clean, bun tests green, synth OK.
</verification>

<success_criteria>
- Both endpoints 401 without JWT, 200 with own data
- Logs include pending + done requests, newest first, capped at 50
</success_criteria>

<output>
After completion, create `.planning/phases/07-dashboard-api/07-02-SUMMARY.md`
</output>