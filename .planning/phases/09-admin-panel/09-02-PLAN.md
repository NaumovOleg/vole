---
phase: 09-admin-panel
plan: 02
type: feature
wave: 2
depends_on: [09-01]
files_modified:
  - ui/src/api.ts
  - ui/src/App.tsx
  - ui/src/Dashboard.tsx
  - ui/src/styles.css
autonomous: true
must_haves:
  truths:
    - "Admin section renders only when me() reports role admin"
    - "Block/unblock round-trips against the live API"
  artifacts:
    - Users admin section in Dashboard
  key_links:
    - "hand-rolled: fetch shape documented in 09-01"
---

<objective>
Surface the admin role in the UI and add a Users section (list, status,
block/unblock) visible only to admins.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-admin-panel/09-PHASE.md
@/Users/oleg/Documents/projects/http-tunell/ui/src/api.ts
@/Users/oleg/Documents/projects/http-tunell/ui/src/App.tsx
@/Users/oleg/Documents/projects/http-tunell/ui/src/Dashboard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: role via me() + admin api helpers</name>
  <files>ui/src/api.ts, ui/src/App.tsx</files>
  <behavior>api.ts: me() unchanged; add adminUsers() → GET /admin/users and
  adminSetBlocked(userId, blocked) → POST `/admin/users/${userId}/${blocked ?
  'block' : 'unblock'}` (request() handles 204). App.tsx: user state gains
  role: string | undefined — from d.role (me()); AuthScreen onAuthed unchanged
  (role filled by the me() call after mount since auth screen is only shown
  logged-out).</behavior>
  <implementation>me() response gains `role` from the backend; App passes role
  through to Dashboard. No comments.</implementation>
  <verify>cd ui && npx tsc --noEmit && npm run build</verify>
  <done>Admin flag reaches the dashboard</done>
</task>

<task type="auto">
  <name>Task 2: Users section</name>
  <files>ui/src/Dashboard.tsx, ui/src/styles.css</files>
  <behavior>If user.role === 'admin': render a Users section between Connections
  and Request logs: table (identifier, status badge active/blocked, created via
  relativeTime, action button Block/Unblock). Data from adminUsers(); action
  calls adminSetBlocked then reloads users. Errors surface in the shared banner.
  Status styling reuses .status classes (add .blocked).</behavior>
  <implementation>Load users alongside tokens/connections/logs inside load()
  only when admin (skip otherwise). Single busy flag per row not needed —
  disable all action buttons while one request is in flight (reuse `busy`).
  No comments.</implementation>
  <verify>cd ui && npx tsc --noEmit && npm run build</verify>
  <done>Block/unblock round-trips; layout stays clean</done>
</task>

</tasks>

<verification>
tsc + build green; manual QA against mock API: non-admin sees no Users section,
admin sees it, block is reflected in list immediately.
</verification>

<success_criteria>
- me() exposes role; App threads it into Dashboard
- Admin-only Users section renders list and block/unblock
- Non-admins never hit the admin endpoints from the UI
</success_criteria>

<output>
After completion, create `.planning/phases/09-admin-panel/09-02-SUMMARY.md`
</output>