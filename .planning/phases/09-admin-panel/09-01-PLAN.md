---
phase: 09-admin-panel
plan: 01
type: feature
wave: 1
depends_on: []
files_modified:
  - infra/lib/lambdas/auth-handler/admin.ts
  - infra/lib/lambdas/auth-handler/index.ts
  - infra/lib/lambdas/ws-handler/index.ts
  - infra/lib/vole-stack.ts
  - infra/test/admin.test.ts
autonomous: true
must_haves:
  truths:
    - "Non-admin requests to /admin/* return 403"
    - "Blocking a user deletes their tokens and tunnels and kills live WS connections"
  artifacts:
    - admin.ts + tests + routes + CDK grants
  key_links:
    - "09-02 renders the admin section from these endpoints"
---

<objective>
Add admin-only endpoints (user list, block, unblock) with an env-flag role,
and make blocking tear down tokens, tunnels, and live WS connections.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-admin-panel/09-PHASE.md
@/Users/oleg/Documents/projects/http-tunell/infra/lib/lambdas/auth-handler/index.ts
@/Users/oleg/Documents/projects/http-tunell/infra/lib/lambdas/ws-handler/index.ts
@/Users/oleg/Documents/projects/http-tunell/infra/lib/vole-stack.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: admin helpers (TDD)</name>
  <files>infra/lib/lambdas/auth-handler/admin.ts, infra/test/admin.test.ts</files>
  <behavior>RED: admin.test.ts — isAdmin(normalizedIdent, adminIdentifiers csv):
  exact match against each csv entry (also normalized), false on empty/missing;
  buildUserList(rows): map to {userId, identifier, blocked, createdAt}, sorted
  createdAt desc; adminRevokePlan(tunnels, connections, userId): unique
  connectionIds (agent tunnels → connectionId, proxy conns → proxyConnectionId,
  ConnectionsTable rows for the user) + subdomains to delete. GREEN: admin.ts
  implements, no comments.</behavior>
  <implementation>Pure module, no AWS SDK — only DDB row shapes from callers.
  Tests with plain objects. bun test.</implementation>
  <verify>cd infra && bun test test/admin.test.ts</verify>
  <done>isAdmin/buildUserList/adminRevokePlan unit-tested</done>
</task>

<task type="auto">
  <name>Task 2: routes + block teardown</name>
  <files>infra/lib/lambdas/auth-handler/index.ts</files>
  <behavior>requireAdmin(event, adminIdentifiers): requireAuth + isAdmin(payload.identifier)
  else 403 'forbidden'. Routes:
  GET /admin/users → Scan USERS_TABLE → buildUserList → {users}.
  POST /admin/users/{id}/block → Get user (404 if missing); Put blocked=true;
  page tokens by userIdIndex & BatchWrite-delete (25/batch, LAST key loop);
  Scan CONNECTIONS_TABLE (full, filter userId in memory) + Query TUNNELS
  by userIdIndex → adminRevokePlan → DeleteConnection (ApiGatewayManagementApi,
  GoneException tolerated); Query tunnels → BatchWrite-delete subdomains
  (paged); return 204.
  POST /admin/users/{id}/unblock → Get (404) → Put blocked=false → 204.
  ADMIN_IDENTIFIERS from process.env; normalized csv once per invocation.</behavior>
  <implementation>Reuse bodyOf/httpError/json/`apiClient` pattern like
  ws-handler index (new ApiGatewayManagementApiClient with AWS_REGION — shared
  region default). Paged delete loop mirrors purgeLogs in ws-handler index.
  No comments.</implementation>
  <verify>cd infra && npx tsc --noEmit && bun test</verify>
  <done>admin routes live; block tears down tokens+tunnels+WS</done>
</task>

<task type="auto">
  <name>Task 3: WS guard + CDK wiring</name>
  <files>infra/lib/lambdas/ws-handler/index.ts, infra/lib/vole-stack.ts</files>
  <behavior>openTunnel: after userId lookup, GetCommand users → if blocked, error
  frame 'account blocked' and return (before the duplicate-tunnel check). CDK:
  authHandler env ADMIN_IDENTIFIERS (prop from context with default
  'admin@vole.sh'); authHandler grantManageConnections via
  webSocketApi.grantManageConnections(authHandler).</behavior>
  <implementation>One GetCommand per tunnel-open — acceptable. No comments.</implementation>
  <verify>cd infra && npx tsc --noEmit && cdk synth && bun test</verify>
  <done>blocked users can't open tunnels; CDK synth green</done>
</task>

</tasks>

<verification>
bun test (incl. new admin.test.ts) green; tsc clean; cdk synth ok.
</verification>

<success_criteria>
- 403 for non-admins on /admin/*, 401 without auth
- Block: user row blocked, tokens empty, tunnels removed, connections killed
- WS: blocked user rejected at $connect and at tunnel-open
</success_criteria>

<output>
After completion, create `.planning/phases/09-admin-panel/09-01-SUMMARY.md`
</output>