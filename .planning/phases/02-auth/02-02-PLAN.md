---
phase: 02-auth
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - infra/lib/vole-stack.ts
  - infra/lib/lambdas/auth-handler/index.ts
  - infra/package.json
autonomous: true
must_haves:
  truths:
    - "User registers with email OR phone + password and gets 201"
    - "User logs in with correct credentials and receives a JWT"
    - "Protected endpoint (GET /auth/me) returns the caller's profile with a valid JWT, 401 without"
    - "Password is stored hashed — no plaintext anywhere"
  artifacts:
    - infra/lib/vole-stack.ts: NodejsFunction ×3, identifierIndex GSI, JwtSecret (SecretsManager), routes POST /auth/register, POST /auth/login, GET /auth/me
    - infra/lib/lambdas/auth-handler/index.ts: router + register/login/me handlers
  key_links:
    - "auth-handler reads JWT secret at cold start from SecretsManager (secretValue, cached)"
    - "Users lookup by identifier via identifierIndex GSI (login path)"
    - "core.ts functions used by register/login handlers"
---

<objective>
Wire auth into the stack and implement register/login/me in the auth-handler Lambda.

Purpose: Delivers AUTH-01 (register email/phone), AUTH-02 (login → JWT), AUTH-05 (JWT-protected endpoint).
Output: Working /auth/register, /auth/login, /auth/me on the HttpApi; CDK updated (NodejsFunction, GSI, secret, routes).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-auth/02-auth-01-SUMMARY.md (only if exists — core.ts exports)
</context>

<tasks>

<task type="auto">
  <name>Task 1: CDK wiring — NodejsFunction, GSI, JWT secret, auth routes</name>
  <files>infra/lib/vole-stack.ts, infra/package.json</files>
  <action>In vole-stack.ts: replace all three lambda.Function with aws-lambda-nodejs NodejsFunction (same construct ids WsHandler/RelayHandler/AuthHandler, runtime NODEJS_22_X, entry `lib/lambdas/<name>/index.ts`, handler 'handler', same env/timeouts, bundling { externalModules: ['@aws-sdk/*'] } — AWS SDK v3 comes from the Lambda runtime). Add to usersTable: `addGlobalSecondaryIndex({ indexName: 'identifierIndex', partitionKey: { name: 'identifier', type: dynamodb.AttributeType.STRING } })`. Create `new secretsmanager.Secret(this, 'JwtSecret', { generateSecretString: { secretStringTemplate: '{}', generateStringKey: 'jwt', excludePunctuation: true } })`; `jwtSecret.grantRead(authHandler)`; add env JWT_SECRET_ARN to authHandler only. Add HttpApi routes: POST /auth/register, POST /auth/login, GET /auth/me → HttpLambdaIntegration to authHandler (keep GET /health). Update infra/package.json: add aws-lambda-nodejs usage (no new dep — it's part of aws-cdk-lib), add devDep esbuild. `npm install`.</action>
  <verify>cd infra && npx tsc --noEmit && npx cdk synth exits 0; template has 3 routes /auth/*, GSI identifierIndex, AWS::SecretsManager::Secret</verify>
  <done>Synth passes with new resources; no errors</done>
</task>

<task type="auto">
  <name>Task 2: Implement register, login, me</name>
  <files>infra/lib/lambdas/auth-handler/index.ts</files>
  <action>Rewrite index.ts: export async handler(event). At module init: read JWT_SECRET_ARN from env, fetch secret once via @aws-sdk/client-secrets-manager (cache in module scope), parse {jwt}. Router on `${event.routeKey || event.requestContext.http.method} ${event.requestContext.http.path}`: route to register/login/me. register: parse JSON body {identifier, password}; validateIdentifier + password ≥8 chars → 400 {error}; check existing via DynamoDB Query on identifierIndex (users table, USERS_TABLE env, @aws-sdk/lib-dynamodb DocumentClient — SDK v3 runtime builtin) → 409 {error: 'already registered'}; put item {userId: randomUUID(), identifier, passwordHash: hashPassword(password), createdAt: Date.now()}; 201 {userId, identifier}. login: query identifierIndex; not found or verifyPassword fails → 401 {error: 'invalid credentials'} (same message both cases, no user enumeration); sign JWT {sub: userId, identifier} 24h expiry; 200 {token, expiresIn: 86400}. me: read Authorization 'Bearer x'; verifyJwt → 401 on any failure; get user by userId → 200 {userId, identifier}; missing → 401. All responses: statusCode, headers {content-type: application/json}, body JSON. Never log passwords or tokens (log only userId/status). Unknown route → 404.</action>
  <verify>npx tsc --noEmit passes; bun test in infra still green; cdk synth exit 0</verify>
  <done>Handler implements the three endpoints with correct status codes and error contracts</done>
</task>

</tasks>

<verification>
Unit: bun test green. Type: tsc clean. Infra: synth clean. Integration (after user deploy): `curl -X POST $HTTP_API/auth/register` returns 201, login returns JWT, /auth/me with token returns profile, without token 401.
</verification>

<success_criteria>
- Register/login/me endpoints exist on HttpApi /auth/* with JWT protection on /me
- Passwords stored as bcrypt hashes only
- Duplicate registration → 409; wrong password → 401 (no enumeration)
</success_criteria>

<output>
After completion, create `.planning/phases/02-auth/02-auth-02-SUMMARY.md`
</output>
