---
phase: 02-auth
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - infra/lib/vole-stack.ts
  - infra/lib/lambdas/auth-handler/index.ts
autonomous: true
must_haves:
  truths:
    - "User creates an API token and sees the full token exactly once"
    - "User lists only their own tokens"
    - "User revokes a token; the token row disappears immediately"
    - "Only the owning user can revoke their token (403 otherwise)"
  artifacts:
    - tokensTable userIdIndex GSI in vole-stack.ts
    - POST /tokens, GET /tokens, DELETE /tokens/{tokenId} in auth-handler/index.ts
  key_links:
    - "token lookup by hash (tokenHashIndex) — used by phase 3 WS $connect auth"
    - "all token routes require valid JWT (requireAuth)"
---

<objective>
Implement API token management: create, list, revoke.

Purpose: Delivers AUTH-03 — tokens the CLI uses to authenticate WS connections (phase 3). Revoked = deleted row, so phase 3 lookups fail immediately.
Output: POST /tokens, GET /tokens, DELETE /tokens/{tokenId} + userIdIndex GSI.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-auth/02-auth-02-SUMMARY.md (only if exists — auth-handler router and requireAuth pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tokens table GSI for per-user listing</name>
  <files>infra/lib/vole-stack.ts</files>
  <action>On tokensTable add: `addGlobalSecondaryIndex({ indexName: 'userIdIndex', partitionKey: { name: 'userId', type: dynamodb.AttributeType.STRING } })`. Verify synth still passes.</action>
  <verify>cd infra && npx cdk synth exits 0; template contains userIdIndex GSI on Tokens table</verify>
  <done>GSI added, synth clean</done>
</task>

<task type="auto">
  <name>Task 2: Token create/list/revoke endpoints</name>
  <files>infra/lib/lambdas/auth-handler/index.ts</files>
  <action>Extend the router with POST /tokens, GET /tokens, DELETE /tokens/{tokenId} (path param from event.requestContext.http.path or rawPath split). Add shared requireAuth(event) helper inside index.ts: verify Bearer JWT → returns {userId, identifier} or throws 401. POST /tokens: auth; generate token = 'vole_' + base64url(crypto.randomBytes(32)); tokenId = randomUUID(); put {tokenId, tokenHash: hashToken(token), userId, createdAt: Date.now()}; 201 {tokenId, token: fullToken} — full token shown once. GET /tokens: auth; Query tokensTable by userIdIndex on userId; 200 {tokens: [{tokenId, createdAt}]} — never return hashes. DELETE /tokens/{tokenId}: auth; Get token by tokenId; not found → 404; item.userId !== caller → 403; else Delete → 204 empty body. Keep register/login/me untouched.</action>
  <verify>npx tsc --noEmit passes; bun test green; cdk synth exit 0</verify>
  <done>All three endpoints implemented with correct ownership checks and status codes</done>
</task>

</tasks>

<verification>
Unit: bun test green. Type: tsc clean. Infra: synth clean. Integration (after user deploy): create token → full token returned once; list shows it; delete → 204; list no longer shows it; cross-user delete → 403.
</verification>

<success_criteria>
- Tokens created with full-value-shown-once contract
- Listing scoped to caller; revocation immediate; ownership enforced
- hashToken used for storage — raw token never persisted
</success_criteria>

<output>
After completion, create `.planning/phases/02-auth/02-auth-03-SUMMARY.md`
</output>
