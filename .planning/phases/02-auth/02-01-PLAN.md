---
phase: 02-auth
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - infra/package.json
  - infra/lib/lambdas/auth-handler/core.ts
  - infra/lib/lambdas/auth-handler/core.test.ts
autonomous: true
must_haves:
  truths:
    - "Passwords are never stored or logged in plaintext — only bcrypt hashes"
    - "JWT signs and verifies with HS256 and a configurable expiry"
    - "API tokens are stored only as sha256 hashes; the raw token exists once at creation"
    - "Identifier validation accepts email OR phone and rejects garbage"
  artifacts:
    - infra/lib/lambdas/auth-handler/core.ts (hashPassword, verifyPassword, signJwt, verifyJwt, hashToken, normalizeIdentifier)
    - infra/lib/lambdas/auth-handler/core.test.ts
  key_links:
    - "core.ts imported by auth-handler/index.ts (plan 02-02)"
    - "bcryptjs + jose added to infra deps"
---

<objective>
Build the pure auth core as TDD: password hashing (bcryptjs), JWT sign/verify (jose HS256), API-token hashing (sha256), identifier normalization/validation (email or phone).

Purpose: All security-sensitive primitives proven by tests before the handler wiring lands. Auth logic is the classic RED-GREEN-REFACTOR candidate.
Output: `core.ts` + `core.test.ts` (bun test), deps bcryptjs + jose.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD auth core</name>
  <files>infra/lib/lambdas/auth-handler/core.ts, infra/lib/lambdas/auth-handler/core.test.ts, infra/package.json</files>
  <behavior>RED: write core.test.ts (run: `bun test` from infra/) with:
  - hashPassword(pw) returns hash !== pw; verifyPassword(pw, hash) true; verifyPassword(wrong, hash) false; hash differs across salts
  - signJwt({userId}, 'secret', {expiresInSec: 3600}) returns JWT; verifyJwt(token, 'secret') returns payload with userId; verifyJwt(token, 'wrong-secret') throws; expired token throws
  - hashToken('vole_abc123') is sha256 hex and deterministic; raw token never equals its hash
  - normalizeIdentifier('  Foo@Bar.COM ') → 'foo@bar.com' (email lowercased/trimmed); phone '+7 912 123-45-67' → '79121234567' (digits only); validateIdentifier returns true for email and phone, false for 'abc', '', 5-char phone</behavior>
  <implementation>GREEN: create core.ts exporting: hashPassword (bcryptjs.hashSync, cost 10), verifyPassword (bcryptjs.compareSync), signJwt (jose SignJWT, HS256, exp set, {sub: userId}), verifyJwt (jose jwtVerify, returns payload, throws on invalid/expired), hashToken (node:crypto createHash sha256 hex), normalizeIdentifier (email → trim+lowercase; phone → strip non-digits), validateIdentifier (email regex OR phone 7-15 digits). Add deps bcryptjs + jose to infra/package.json and `npm install`. RED commit after test exists, GREEN commit after core.ts passes.</implementation>
  <verify>cd infra && bun test — all green; `bun test --bail` exits 0</verify>
  <done>All behaviors pass; core.ts exports the six functions; deps installed</done>
</task>

</tasks>

<verification>
bun test passes fully. Typecheck: `npx tsc --noEmit` in infra/ passes.
</verification>

<success_criteria>
- bcryptjs hashing round-trips, rejects wrong passwords, unique salts
- JWT HS256 round-trips, rejects wrong secret and expired tokens
- Token hashing is one-way and deterministic
- Email/phone normalization and validation behave as specified
</success_criteria>

<output>
After completion, create `.planning/phases/02-auth/02-auth-01-SUMMARY.md`
</output>
