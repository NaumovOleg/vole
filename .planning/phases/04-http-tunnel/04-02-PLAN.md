---
phase: 04-http-tunnel
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - infra/lib/lambdas/ws-handler/index.ts
  - infra/lib/vole-stack.ts
autonomous: true
must_haves:
  truths:
    - "CLI requests a tunnel with tunnel-open → server assigns a unique subdomain and replies with the public URL"
    - "Second tunnel-open on same connection is rejected; subdomains never collide"
    - "CLI response frames land in LogsTable (pending → done + chunk items) so the relay can pick them up"
    - "Disconnect removes the user's tunnel rows and pending requests"
  artifacts:
    - ws-handler: tunnel-open/tunnel-close/response/data handling
    - vole-stack.ts: TunnelsTable userIdIndex GSI, relay manageConnections grant
  key_links:
    - "relay (04-03) reads LogsTable items written here"
    - "subdomain scheme u-<userId8>[-n] enforced here"
---

<objective>
Extend ws-handler with tunnel lifecycle (tunnel-open/close) and CLI-response ingestion (response/data frames → LogsTable). CDK: tunnels GSI + relay manageConnections.

Purpose: The server half of the loop — assigns public URLs and turns CLI responses into pollable state.
Output: ws-handler tunnel+response handling; stack updates.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ws-control/03-ws-control-02-SUMMARY.md (ws-handler)
@.planning/phases/04-http-tunnel/04-http-tunnel-01-SUMMARY.md (protocol frames)
</context>

<tasks>

<task type="auto">
  <name>Task 1: CDK — tunnels GSI, relay manageConnections</name>
  <files>infra/lib/vole-stack.ts</files>
  <action>Add to tunnelsTable: `addGlobalSecondaryIndex({ indexName: 'userIdIndex', partitionKey: { name: 'userId', type: dynamodb.AttributeType.STRING } })`. Add `webSocketApi.grantManageConnections(relayHandler);` next to the existing ws-handler grant. Verify synth passes.</action>
  <verify>cd infra && npx cdk synth exits 0; template has tunnels userIdIndex + relay IAM policy for execute-api:ManageConnections</verify>
  <done>Stack updated, synth clean</done>
</task>

<task type="auto">
  <name>Task 2: Tunnel lifecycle in ws-handler</name>
  <files>infra/lib/lambdas/ws-handler/index.ts</files>
  <action>Extend the $default frame switch (TUNNELS_TABLE + LOGS_TABLE already in env; add table refs):
  - 'tunnel-open': payload {type, localPort}. Verify caller's connection row exists (GetItem CONNECTIONS_TABLE; userId from it). Determine subdomain: base = 'u-' + userId.slice(0, 8); try conditional Put {subdomain: base, userId, connectionId, type, localPort, createdAt} → on ConditionalCheckFailedException retry base + '-' + i (i=1..9, else error frame 'no subdomain slots'). On success reply tunnel-open frame {subdomain, url: 'https://' + subdomain + '.vole.sh'}. If connection already has a tunnel (query TUNNELS by connectionId? add GSI — NO: query by userIdIndex then filter connectionId, or simpler: attempt conditional put; if the connection already owns a tunnel return error frame 'tunnel already open' — detect by querying userIdIndex with connectionId match). Reply errors as error frame with the request id.
  - 'tunnel-close': payload {subdomain}; delete row if owned by caller's connection (GetItem, check connectionId), else error frame.
  - 'response': payload {id, statusCode, headers, chunkTotal?, bodyB64?}. Update LOGS_TABLE item {connectionId, requestId: id}: SET response = {statusCode, headers, chunkTotal, bodyB64}, status = 'done' (UpdateCommand; if item missing, Create with full item). Body > chunkable → CLI already sent chunks; bodyB64 only for single-frame case.
  - 'data': payload {id, n, data}. Put LOGS_TABLE item {connectionId, requestId: id + '#' + n, data} (chunk items; TTL same 60s window: expiresAt = now + 60000).
  - $disconnect: also delete tunnel rows for the connection (query TUNNELS userIdIndex → need connectionId... rows keyed by subdomain: scan by userIdIndex then delete matching connectionId rows) and delete pending request items? Leave pending items to TTL (60s) — no cleanup needed.
  Log only ids, never bodies or tokens.</action>
  <verify>npx tsc --noEmit && bun test green && npx cdk synth exit 0</verify>
  <done>tunnel-open/close and response/data implemented with ownership checks</done>
</task>

</tasks>

<verification>
Typecheck + tests + synth clean. End-to-end via 04-03/04-05 after deploy.
</verification>

<success_criteria>
- Unique subdomain per tunnel; ownership enforced on close
- Response+chunks persisted for relay polling; TTL 60s bounds stale items
</success_criteria>

<output>
After completion, create `.planning/phases/04-http-tunnel/04-http-tunnel-02-SUMMARY.md`
</output>
