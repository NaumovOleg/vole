---
phase: 04-http-tunnel
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - shared/src/protocol.ts
  - shared/src/protocol.test.ts
  - shared/src/chunks.ts
  - shared/src/chunks.test.ts
autonomous: true
must_haves:
  truths:
    - "HTTP request/response travel as typed frames with correlation ids"
    - "Bodies ≤90KB fit one frame; larger bodies split into 90KB chunks and reassemble losslessly"
    - "Chunking is binary-safe (base64 in frames)"
  artifacts:
    - shared/src/protocol.ts: request/response/data frame types + builders
    - shared/src/chunks.ts: splitBody/assembleBody
    - shared/src/chunks.test.ts, extended protocol.test.ts
  key_links:
    - "relay-handler builds request frames (04-03); CLI assembles them (04-05)"
---

<objective>
Extend the frame protocol with HTTP request/response/data frames and binary-safe chunking (TDD).

Purpose: PROTO-02 — bodies >128KB API Gateway frame limit must survive the tunnel. One shared implementation for relay + CLI.
Output: protocol builders for request/response/data; chunks.ts split/assemble; tests.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ws-control/03-ws-control-01-SUMMARY.md (protocol)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD request/response frames + chunking</name>
  <files>shared/src/protocol.ts, shared/src/protocol.test.ts, shared/src/chunks.ts, shared/src/chunks.test.ts</files>
  <behavior>RED: extend shared/src/protocol.test.ts and add chunks.test.ts (bun test in shared/):
  - requestFrame(id, {method, path, query, headers, bodyB64?}) → frame t=request with d{method, path, query, headers, bodyB64}
  - requestFrame with chunkTotal when bodyB64 absent but bodyChunkTotal set: d{chunkTotal}
  - responseFrame(id, {statusCode, headers, bodyB64?, chunkTotal?}) → t=response
  - dataFrame(id, n, b64) → t=data, d{n, data}
  - CHUNK_SIZE = 90 * 1024
  - splitBody(b64): 200KB buffer (base64 string) → chunks array, all ≤ CHUNK_SIZE after base64, reassembly via assembleBody returns original bytes
  - empty body → splitBody returns []; assembleBody([]) → ''
  - 90KB-exact body → exactly 1 chunk; 90KB+1 → 2 chunks
  - binary safety: Uint8Array with 0x00..0xFF round-trips via splitBody/assembleBody</behavior>
  <implementation>GREEN: in shared/src/protocol.ts add builders requestFrame/responseFrame/dataFrame (payload shapes above). New shared/src/chunks.ts: CHUNK_SIZE constant (90*1024), splitBody(bodyB64: string): string[] — split base64 string so each chunk stays under CHUNK_SIZE raw (use byte length: slice buffer at CHUNK_SIZE, re-encode each slice), assembleBody(chunks: string[]): string — concat + validate nothing (JSON-level). Export types HttpRequestPayload, HttpResponsePayload, DataPayload. No comments.</implementation>
  <verify>cd shared && bun test — all green</verify>
  <done>Protocol builders and chunk helpers proven by tests</done>
</task>

</tasks>

<verification>
bun test passes fully in shared/.
</verification>

<success_criteria>
- request/response/data frames round-trip with correct payload shape
- Chunking: size cap honored, binary-safe, exact-boundary behavior proven
</success_criteria>

<output>
After completion, create `.planning/phases/04-http-tunnel/04-http-tunnel-01-SUMMARY.md`
</output>
