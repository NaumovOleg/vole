---
phase: 05-tcp-ws-tunnels
plan: 04
type: tdd
wave: 2
depends_on: [05-01]
files_modified:
  - cli/src/commands/ws.ts
  - cli/src/index.ts
  - cli/src/bridge.ts (optional reuse)
  - cli/README.md
autonomous: true
must_haves:
  truths:
    - "`vole ws <port>` proxies WS messages to a local WS server"
    - "Local WS message → data frame → remote; remote bytes → local WS message"
  artifacts:
    - cli/src/commands/ws.ts: WS proxy command
    - cli/src/wsp-bridge.test.ts: message-level bridge tests
  key_links:
    - "remote side attaches via ?tunnel= (05-02); byte bridge from 05-03"
---

<objective>
`vole ws <port>`: when a remote client attaches to the tunnel, connect to the local WS
server at ws://localhost:<port> and proxy messages between remote and local WS as
data frames.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-tcp-ws-tunnels/05-PHASE.md
@cli/src/tunnel.ts
@cli/src/bridge.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WS proxy bridge (TDD)</name>
  <files>cli/src/ws-bridge.ts, cli/src/ws-bridge.test.ts</files>
  <behavior>RED: add cli/src/ws-bridge.test.ts (bun test): with a local WS server
  (bun's built-in WebSocketServer on ws://localhost:PORT):
  - WsBridge(localWs, send): local message (text) → send({n, data: b64})
  - incoming data frame → localWs.send(decoded bytes as binary)
  - local WS close → onClose; error → onError
  - message ordering preserved under rapid sends
  - binary local message round-trips byte-exact</behavior>
  <implementation>GREEN: cli/src/ws-bridge.ts — class WsBridge: wraps the agent's WS client
  to the local server; message → data frame (base64); data frame → ws.send(Uint8Array).
  Sequence counter shared per direction. No comments.</implementation>
  <verify>cd cli && bun test</verify>
  <done>WS message bridge proven with a real local WS server</done>
</task>

<task type="auto">
  <name>Task 2: vole ws command</name>
  <files>cli/src/commands/ws.ts, cli/src/index.ts, cli/README.md</files>
  <behavior>vole ws <port>: TunnelSession type 'ws', on tunnel-open print the attach URL
  `wss://api.vole.sh/dev?tunnel=<subdomain>`. On first data frame (or tunnel-open),
  connect WS client to ws://localhost:<port> (bun's WebSocket). WsBridge routes frames.
  SIGINT → close. index.ts routes 'ws'. README: add tcp/ws commands + websocat example.</behavior>
  <implementation>Wire onData hook (shared with tcp). Reconnect on local WS failure? no —
  error frame + keep tunnel; document. No comments.</implementation>
  <verify>cd cli && npx tsc --noEmit && bun test</verify>
  <done>vole ws works end-to-end at code level</done>
</task>

</tasks>

<verification>
tsc clean, bun tests green in cli/.
</verification>

<success_criteria>
- WS message proxy proven against a real local WS server (text + binary)
- `vole ws <port>` command wired with URL output
</success_criteria>

<output>
After completion, create `.planning/phases/05-tcp-ws-tunnels/05-04-SUMMARY.md`
</output>
