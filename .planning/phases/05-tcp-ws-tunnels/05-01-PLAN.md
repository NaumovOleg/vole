---
phase: 05-tcp-ws-tunnels
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - infra/lib/lambdas/ws-handler/routing.ts
  - infra/lib/lambdas/ws-handler/routing.test.ts
autonomous: true
must_haves:
  truths:
    - "Tunnel routing decisions are pure and unit-testable"
    - "Byte-stream data frames route to the right connection deterministically"
  artifacts:
    - infra/lib/lambdas/ws-handler/routing.ts: pure routing helpers
    - infra/lib/lambdas/ws-handler/routing.test.ts
  key_links:
    - "ws-handler uses routing helpers (05-02); CLI bridges bytes (05-03/05-04)"
---

<objective>
Extract tunnel routing decisions into a pure, tested module: which connection receives
data frames, and how a proxy connection maps to its tunnel.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-tcp-ws-tunnels/05-PHASE.md
@infra/lib/lambdas/ws-handler/index.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD routing helpers</name>
  <files>infra/lib/lambdas/ws-handler/routing.ts, infra/lib/lambdas/ws-handler/routing.test.ts</files>
  <behavior>RED: add infra/test/routing.test.ts (bun test in infra/). Cover:
  - isProxyConnection(connRow): true iff role === 'proxy'
  - byteStreamTunnel(tunnelRow): true iff type is 'tcp' or 'ws' (http → false)
  - proxyTarget(subdomain, tunnels): returns the active tunnel row for a subdomain
  - nextSeq(prev): monotonic counter wrap-safe (Number.MAX_SAFE_INTEGER → 0)
  - forwardBytesToAgent(proxyRow, tunnels): agent connectionId for the proxy's tunnelSubdomain
  - classifyDataFrame(connRow, tunnelRow): 'proxy-to-agent' | 'agent-bytes' | 'http-chunk'</behavior>
  <implementation>GREEN: new infra/lib/lambdas/ws-handler/routing.ts with the pure functions above.
  Rows are plain objects; routing.ts imports nothing AWS — no SDK imports. classifyDataFrame:
  proxy row → 'proxy-to-agent'; agent row with byte-stream tunnel → 'agent-bytes'; agent row
  with http tunnel → 'http-chunk'; unknown → 'ignore'. No comments.</implementation>
  <verify>cd infra && bun test — all green</verify>
  <done>Routing decisions proven by tests, no AWS imports in routing.ts</done>
</task>

</tasks>

<verification>
bun test passes fully in infra/.
</verification>

<success_criteria>
- All routing classification helpers covered by tests
- routing.ts is pure (importable in unit tests without AWS SDK)
</success_criteria>

<output>
After completion, create `.planning/phases/05-tcp-ws-tunnels/05-01-SUMMARY.md`
</output>
