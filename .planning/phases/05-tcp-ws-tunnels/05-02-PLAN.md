---
phase: 05-tcp-ws-tunnels
plan: 02
type: integration
wave: 2
depends_on: [05-01]
files_modified:
  - infra/lib/lambdas/ws-handler/index.ts
  - infra/lib/vole-stack.ts
autonomous: true
must_haves:
  truths:
    - "Proxy connections attach via ?tunnel=<subdomain> and route bytes to the agent"
    - "Agent data frames from tcp/ws tunnels reach the proxy as raw bytes"
    - "Binary round-trips without corruption"
  artifacts:
    - infra/lib/lambdas/ws-handler/index.ts: proxy $connect, byte routing in $default
    - infra/lib/vole-stack.ts: WS API binaryMediaTypes
  key_links:
    - "routing helpers from 05-01; CLI bridges on the other end (05-03/05-04)"
---

<objective>
Wire proxy connections into the WS API: subdomain-based attach, byte forwarding both
directions, binary media types, cleanup on disconnect.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-tcp-ws-tunnels/05-PHASE.md
@infra/lib/lambdas/ws-handler/index.ts
@infra/lib/vole-stack.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Proxy $connect via ?tunnel=<subdomain></name>
  <files>infra/lib/lambdas/ws-handler/index.ts</files>
  <behavior>onConnect: if queryStringParameters.tunnel is present, treat as proxy connect —
  NO token auth (subdomain is the capability). Look up TunnelsTable by subdomain; missing →
  404. Tunnel type http → 400 (http tunnels use the relay). Store connection row with
  role='proxy', tunnelSubdomain=subdomain, connectedAt/lastSeenAt/expiresAt (same TTL as agent).
  Agent connections keep current token path.</behavior>
  <implementation>Branch in onConnect before token logic. Reuse touch/ttl helpers. No comments.</implementation>
  <verify>npx tsc --noEmit && npx cdk synth</verify>
  <done>Proxy connect flow implemented</done>
</task>

<task type="auto">
  <name>Task 2: $default byte routing</name>
  <files>infra/lib/lambdas/ws-handler/index.ts</files>
  <behavior>onMessage: look up the connection row. Cases (use routing.ts helpers):
  - proxy row: incoming body is raw bytes (text string or base64 via isBase64Encoded) →
    post data frame {n, data: b64} to the agent connectionId (from tunnel row).
    Missing agent → GoneException-like cleanup (delete proxy row), reply nothing.
  - agent row with tcp/ws tunnel (classify 'agent-bytes'): 'data' frame body {n, data:b64}
    → decode to Uint8Array → post raw bytes to the proxy connection.
  - agent http tunnel: 'data' frame → existing storeChunk (unchanged).
  Sequence n: track per direction (proxy→agent, agent→proxy) in a small in-memory map
  keyed by connectionId — restart-safe (only ordering hint). Malformed body → error frame.
  hello/tunnel-open from proxy connections → error frame 'protocol violation'.</behavior>
  <implementation>Modify onMessage switch: add proxy handling for 'data' case + intercept
  agent 'data' when tunnel is byte-stream. safePost extended: accept Uint8Array for binary.
  Decode incoming: event.isBase64Encoded ? Buffer.from(event.body, 'base64') : Buffer.from(event.body, 'utf8').
  No comments.</implementation>
  <verify>npx tsc --noEmit && npx cdk synth && bun test</verify>
  <done>Byte routing both directions implemented</done>
</task>

<task type="auto">
  <name>Task 3: $disconnect + CDK binary media types</name>
  <files>infra/lib/lambdas/ws-handler/index.ts, infra/lib/vole-stack.ts</files>
  <behavior>onDisconnect: proxy connection → delete its row (no tunnels to clean — proxy owns
  none). Agent disconnect unchanged. CDK: WebSocketApi gains binaryMediaTypes:
  ['application/octet-stream'] (binary frames reach the handler; outgoing binary frames work).</behavior>
  <implementation>Disconnect branch + stack property. No comments.</implementation>
  <verify>npx tsc --noEmit && npx cdk synth</verify>
  <done>Cleanup and binary config in place</done>
</task>

</tasks>

<verification>
tsc clean, cdk synth succeeds, existing bun tests still green.
</verification>

<success_criteria>
- Proxy connection attaches by subdomain and receives/returns bytes to/from the agent
- Agent byte-stream data frames reach the proxy as raw WS bytes
- Binary media types configured on the WS API
</success_criteria>

<output>
After completion, create `.planning/phases/05-tcp-ws-tunnels/05-02-SUMMARY.md`
</output>
