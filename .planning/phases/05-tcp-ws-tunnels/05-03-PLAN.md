---
phase: 05-tcp-ws-tunnels
plan: 03
type: tdd
wave: 2
depends_on: [05-01]
files_modified:
  - cli/src/tunnel.ts
  - cli/src/commands/http.ts
  - cli/src/commands/tcp.ts
  - cli/src/index.ts
autonomous: true
must_haves:
  truths:
    - "`vole tcp <port>` bridges a TCP socket to data frames over WS"
    - "Arbitrary bytes (incl. binary) travel uncorrupted in both directions"
  artifacts:
    - cli/src/commands/tcp.ts: TCP bridge command
    - cli/src/tunnel.ts: data-frame handling for byte-stream tunnels
    - cli/src/bridge.test.ts
  key_links:
    - "server side routes bytes (05-02); `vole ws` shares the bridge (05-04)"
---

<objective>
`vole tcp <port>`: open a TCP socket to localhost:<port> and bridge bytes with the tunnel
via data frames (base64). Verified with a local test server.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-tcp-ws-tunnels/05-PHASE.md
@cli/src/tunnel.ts
@cli/src/commands/http.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TCP bridge core (TDD)</name>
  <files>cli/src/bridge.ts, cli/src/bridge.test.ts</files>
  <behavior>RED: add cli/src/bridge.test.ts (bun test in cli/, no network needed beyond localhost):
  - ByteBridge with a fake send(): socket 'data' → send({n, data: b64}) frames with increasing n
  - incoming data frame {n, data} → socket.write(decoded bytes)
  - buffer boundaries: chunked writes of a 300KB binary buffer round-trip unchanged
  - socket 'error' → onError callback; socket 'close' → onClose callback
  - backpressure: pause socket when send() is slow? no — serialize via promise chain, drop nothing</behavior>
  <implementation>GREEN: cli/src/bridge.ts — class ByteBridge(socket, send): writes decoded
  frames to socket; on socket data → send({n, data}). Sequence via nextSeq-like counter.
  socket.setNoDelay(true). No comments.</implementation>
  <verify>cd cli && bun test</verify>
  <done>ByteBridge proven: binary round-trip, sequencing, error hooks</done>
</task>

<task type="auto">
  <name>Task 2: vole tcp command</name>
  <files>cli/src/commands/tcp.ts, cli/src/commands/http.ts, cli/src/index.ts</files>
  <behavior>vole tcp <port>: load config, TunnelSession type 'tcp', on tunnel-open print
  `Vole ready: wss://api.vole.sh/dev?tunnel=<subdomain>` (the attach URL; also print
  websocat example). net.connect(localhost:port) → ByteBridge(socket, (d) => session.sendData(d)).
  TunnelSession gains sendData({n,data}) + dispatches 'data' frames to the active bridge
  (agent-side byte-stream tunnels only). Socket connect failure → error frame to server + exit.
  SIGINT → tunnel-close + socket destroy. index.ts routes 'tcp'.</behavior>
  <implementation>Wire through TunnelSession: onRequest stays http-only; add onData hook.
  dispatch 'data': if byte-stream tunnel active → bridge.write(data). No comments.</implementation>
  <verify>cd cli && npx tsc --noEmit && bun test</verify>
  <done>vole tcp works end-to-end at code level</done>
</task>

</tasks>

<verification>
tsc clean, bun tests green in cli/.
</verification>

<success_criteria>
- TCP socket ↔ data-frame bridge proven by tests (binary-safe)
- `vole tcp <port>` command wired with URL output and websocat hint
</success_criteria>

<output>
After completion, create `.planning/phases/05-tcp-ws-tunnels/05-03-SUMMARY.md`
</output>
