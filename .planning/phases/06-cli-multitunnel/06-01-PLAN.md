---
phase: 06-cli-multitunnel
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - cli/src/manager.ts
  - cli/src/manager.test.ts
  - cli/src/index.ts
autonomous: true
must_haves:
  truths:
    - "One process runs N tunnels, each with its own session, all independent"
    - "A failing tunnel (connect error, tunnel-open error) never kills the process"
  artifacts:
    - cli/src/manager.ts: TunnelManager registry + lifecycle
    - cli/src/manager.test.ts
    - cli/src/index.ts: multi-arg parsing
  key_links:
    - "06-02 hardens error paths for CLI-07"
---

<objective>
TunnelManager: register N tunnel specs, start them concurrently, track per-tunnel
state, expose close-all. CLI parses repeated `http/tcp/ws <port>` args.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-cli-multitunnel/06-PHASE.md
@cli/src/index.ts
@cli/src/tunnel.ts
@cli/src/commands/http.ts
@cli/src/commands/tcp.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TunnelManager (TDD)</name>
  <files>cli/src/manager.ts, cli/src/manager.test.ts</files>
  <behavior>RED: add cli/src/manager.test.ts (bun test, no real network — inject factories):
  - addTunnel(spec) registers; start() launches each via injected `launch` fn
  - state transitions: 'pending' → 'starting' → 'ready' | 'failed'; readyCallback per tunnel
  - one tunnel failing (launch throws / marks failed) does not prevent others from starting
  - closeAll() calls close on every started tunnel and resolves when all closed
  - getById/tunnels list for reporting</behavior>
  <implementation>GREEN: cli/src/manager.ts — class TunnelManager<T> with
  specs [{id, kind, port, launch, onReady, onError}]; launch resolves when tunnel
  ready or rejects with Error; state machine as above; failures logged via onError,
  process never exits. No comments.</implementation>
  <verify>cd cli && bun test</verify>
  <done>TunnelManager proven: concurrent start, independent failure, close-all</done>
</task>

<task type="auto">
  <name>Task 2: multi-arg CLI parsing</name>
  <files>cli/src/index.ts</files>
  <behavior>Parse process.argv: sequence of `http|tcp|ws <port>` pairs (any count ≥1).
  Build specs, create TunnelManager, wire onReady to print each tunnel's URL line as
  it arrives (same print as today), onError prints `error: <slot> ...`. Exit 2 on
  malformed args (odd count, bad port). Empty args → usage.</behavior>
  <implementation>Refactor commands/http|tcp|ws into `launch` factories returning
  {session, ready: Promise, close} used by TunnelManager; keep print formats.
  Behavior unchanged for existing single-tunnel invocations. No comments.</implementation>
  <verify>cd cli && npx tsc --noEmit && bun test</verify>
  <done>Multi-tunnel CLI parsing + manager integration working</done>
</task>

</tasks>

<verification>
tsc clean, bun tests green in cli/.
</verification>

<success_criteria>
- Multiple tunnels start concurrently and independently
- Failing tunnel doesn't kill the process
</success_criteria>

<output>
After completion, create `.planning/phases/06-cli-multitunnel/06-01-SUMMARY.md`
</output>