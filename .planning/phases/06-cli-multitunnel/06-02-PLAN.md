---
phase: 06-cli-multitunnel
plan: 02
type: tdd
wave: 2
depends_on: [06-01]
files_modified:
  - cli/src/tunnel.ts
  - cli/src/commands/http.ts
  - cli/src/commands/tcp.ts
  - cli/src/commands/ws.ts
  - cli/src/errors.ts
  - cli/src/errors.test.ts
  - cli/README.md
autonomous: true
must_haves:
  truths:
    - "Every failure path produces a human-readable message with a suggestion"
    - "Exit codes: 2 usage/config, 1 runtime; 0 on clean Ctrl+C"
  artifacts:
    - cli/src/errors.ts: message formatting + exit codes
    - cli/src/errors.test.ts
    - all commands: normalized error paths
  key_links:
    - "TunnelManager from 06-01 swallows per-tunnel failure; this plan makes messages right"
---

<objective>
CLI-07: all failure modes get clear messages — bad/no token, unreachable server,
local port down, tunnel-open rejection; signals close everything gracefully.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-cli-multitunnel/06-PHASE.md
@cli/src/index.ts
@cli/src/tunnel.ts
@cli/src/manager.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: error formatter (TDD)</name>
  <files>cli/src/errors.ts, cli/src/errors.test.ts</files>
  <behavior>RED: cli/src/errors.test.ts:
  - formatError('no-token') → includes "authtoken"
  - formatError('bad-token') → includes "authtoken", exit code 2
  - formatError('network', {server}) → includes server + "network", exit 1
  - formatError('port-down', {port}) → includes port + "not listening", exit 1
  - formatError('tunnel-rejected', {message}) → server's error text, exit 1
  - exit codes map: no-token/bad-token=2, others=1</behavior>
  <implementation>GREEN: cli/src/errors.ts — kind→{message, exitCode} mapping + formatError(kind, ctx).
  No comments.</implementation>
  <verify>cd cli && bun test</verify>
  <done>Error messages + exit codes proven</done>
</task>

<task type="auto">
  <name>Task 2: wire error paths + signals</name>
  <files>cli/src/tunnel.ts, cli/src/commands/http.ts, cli/src/commands/tcp.ts, cli/src/commands/ws.ts, cli/src/index.ts, cli/README.md</files>
  <behavior>
  - TunnelSession: WS 'error' before openPromise resolves → classify network error
    (server from config in message); openPromise rejects instead of infinite hang —
    add a ready timeout (15s) → reject 'network'
  - token missing/blank → no-token at parse time; tunnel-open error frame (e.g.
    auth fail) → bad-token / tunnel-rejected
  - control frame 'error' → resolve/reject openPromise with its message
  - local socket/WS connect failure in tcp/ws commands → port-down (still per-tunnel)
  - index.ts: single SIGINT/SIGTERM handler → manager.closeAll() → "closed N tunnel(s)" → exit 0
  - README: multi-tunnel usage `vole http 3000 http 4000 tcp 5000` + exit codes table</behavior>
  <implementation>All messages via formatError. Keep single-tunnel UX identical.
  No comments.</implementation>
  <verify>cd cli && npx tsc --noEmit && bun test && npm run build && node dist/vole.js --help</verify>
  <done>Every failure path human-readable; signals clean-exit</done>
</task>

</tasks>

<verification>
tsc clean, bun tests green, help output shows multi-tunnel usage.
</verification>

<success_criteria>
- Bad token / no network / port down / rejected tunnel: clear messages + right exit codes
- Ctrl+C closes all tunnels, exit 0
</success_criteria>

<output>
After completion, create `.planning/phases/06-cli-multitunnel/06-02-SUMMARY.md`
</output>