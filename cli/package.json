{
  "name": "vole-tunell",
  "version": "0.1.0",
  "description": "Self-hosted HTTP tunnel client (ngrok alternative on AWS)",
  "type": "module",
  "bin": {
    "vole": "dist/vole.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "bun build src/index.ts --target=node --outfile dist/vole.js --banner \"#!/usr/bin/env node\"",
    "prepack": "npm run build"
  },
  "devDependencies": {
    "@tunell/shared": "*",
    "typescript": "^5.0.0"
  }
}
